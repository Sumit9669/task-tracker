/**
 * User role enum
 */
export enum TaskStatusEnum {
  open = 0,
  active = 1,
  completed = 2,
}

/**
 * Http Status Code Enum
 */
export enum HttpStatusCodes {
  badRequest = 400,
  notFound = 404,
  internalServerError = 500,
  unauthorized = 401,
  ok = 200,
}
