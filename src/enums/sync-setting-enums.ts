export enum SyncFrequencyFor {
    AllBusinesses = 1,
    NewBusinesses = 2
}

// Here on RHS we have number of hours
export enum SyncIntervals {
    ThreeHours = 3,
    OneDay = 24,
    OneWeek = 168
}

// Default pagination values
export enum BusinessSyncSettingDefaultParams {
    pageNumber = 1,
    pageSize = 10

}